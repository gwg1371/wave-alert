# Wave Alert — Claude Instructions

## Git workflow

After pushing a branch, always:
1. Create a PR from that branch into `main`
2. Merge the PR immediately (squash merge) without asking for confirmation
